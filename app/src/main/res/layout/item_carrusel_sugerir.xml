<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="110dp"
    android:layout_height="130dp"
    android:layout_marginEnd="@dimen/spacing_s"
    app:cardCornerRadius="12dp"
    app:cardElevation="@dimen/elevation_medium"
    app:cardBackgroundColor="@color/fondo_secundario">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="8dp">

        <!-- Icono de disco (fijo) -->
        <ImageView
            android:id="@+id/ivPortada"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@drawable/ic_disco"
            android:scaleType="centerCrop"
            android:padding="4dp" />

        <!-- Contenedor para texto con altura fija -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center_vertical"
            android:layout_marginTop="4dp">

            <!-- Contador (esquina, se coloca con FrameLayout si quieres, pero por ahora va aquí) -->
            <TextView
                android:id="@+id/tvContador"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:background="@color/accento_principal"
                android:textColor="@color/fondo_principal"
                android:textSize="10sp"
                android:textStyle="bold"
                android:paddingHorizontal="6dp"
                android:paddingVertical="2dp"
                android:visibility="gone"
                android:layout_marginBottom="2dp" />

            <!-- Título (con máximo 2 líneas, no expande) -->
            <TextView
                android:id="@+id/tvTitulo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/texto_primario"
                android:textSize="11sp"
                android:textStyle="bold"
                android:maxLines="2"
                android:ellipsize="end"
                android:gravity="center_horizontal"
                android:lineSpacingExtra="2dp" />

            <!-- Artista (1 línea) -->
            <TextView
                android:id="@+id/tvArtista"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/texto_secundario"
                android:textSize="9sp"
                android:maxLines="1"
                android:ellipsize="end"
                android:gravity="center_horizontal" />

        </LinearLayout>

    </LinearLayout>
</androidx.cardview.widget.CardView>